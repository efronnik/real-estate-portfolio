{
  "name": "real-estate-portfolio",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "NITRO_NO_WORKER=1 CHOKIDAR_USEPOLLING=1 WATCHPACK_POLLING=true nuxt dev --no-fork --host 127.0.0.1 --port 3000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.4.2",
    "vivus": "^0.4.6",
    "vue": "^3.5.32",
    "vue-router": "^5.0.4"
  }
}
